import React from "react";

const SearchBar = (props)=>{
    return(
        <div>
            <button>Burcar</button>
            <input type="search" />
        </div>
    )
}

export default SearchBar;